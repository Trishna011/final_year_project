version: '3.8'

services:
  flask-api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5001:5001"

  node-server:
    build:
      context: .
      dockerfile: Dockerfile.node
    ports:
      - "4000:4000"
    depends_on:
      - flask-api
